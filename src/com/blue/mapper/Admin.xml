<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.blue.mapper.AdminMapper">
	    <select id="check" resultType="int">
	        select admin_id from  admin  where admin_name=#{admin_name}  and admin_name=#{admin_password}  
	    </select>    
	    

	</mapper>

